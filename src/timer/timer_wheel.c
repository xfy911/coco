/**
 * timer_wheel.c - 时间轮定时器
 */

#include "coco.h"

coco_timer_t *coco_timer(uint64_t delay_ms, void (*callback)(void*), void *arg) {
    return NULL;
}

void coco_timer_cancel(coco_timer_t *timer) {
}